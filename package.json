{
  "name": "splitbee-proxy",
  "version": "1.0.0",
  "description": "Minimal proxy that allows you to run a proxy to bypass ad blockers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Tobias Lins <me@tobi.sh>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
